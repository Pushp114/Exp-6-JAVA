// StudentDAO.java (Model Layer)
public class StudentDAO {
    // CRUD methods go here, using PreparedStatement.
    
    public void addStudent(Student student) {
        String sql = "INSERT INTO student (name, age, city) VALUES (?, ?, ?)";
        // ... JDBC code to set parameters (student.getName(), etc.) and execute ...
    }
    
    public List<Student> getAllStudents() {
        // ... JDBC code to SELECT all students and map ResultSet rows to Student objects ...
        return studentList;
    }
    
    // ... updateStudent() and deleteStudent() methods ...
}
